<?xml version="1.0" encoding="utf-8"?>
<!-- Soft amber hint pill behind the "Milo recognized X%" tip on the scan preview -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/brand_primary_tint" />
    <stroke android:width="1dp" android:color="@color/brand_primary_soft" />
    <corners android:radius="@dimen/radius_sm" />
</shape>
